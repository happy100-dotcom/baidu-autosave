# Required: Generate a random 64-character hex string
#   python3 -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=change_me_to_a_random_64_character_hex_string

# Optional: Set initial admin password (only used when no password_hash exists)
# Minimum 8 characters recommended
ADMIN_PASSWORD=change_me_to_your_admin_password